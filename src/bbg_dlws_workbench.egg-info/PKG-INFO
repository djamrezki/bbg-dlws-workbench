Metadata-Version: 2.4
Name: bbg-dlws-workbench
Version: 0.1.0
Summary: A workbench for Bloomberg Data License Web Services (DLWS): build requests, poll responses, convert to CSV.
Author-email: Djamel <you@example.com>
Requires-Python: >=3.12
Description-Content-Type: text/markdown
Requires-Dist: typer>=0.12
Requires-Dist: pydantic>=2.7
Requires-Dist: pyyaml>=6.0.1
Requires-Dist: zeep>=4.3.1
Requires-Dist: cryptography>=43.0.0
Requires-Dist: requests>=2.32.2


# bbg-dlws-workbench

Python 3.12 workbench for Bloomberg Data License Web Services (DLWS).

## Highlights
- Config-driven runs
- Identifiers from CSV *or* inline
- Fields from YAML *or* a text file
- Chunking for huge identifier lists
- Async submit → poll with configurable attempts/interval/timeout
- Pluggable Store (filesystem by default; S3 planned)
- p12 client certificate auth to DLWS (WSDL default: https://service.bloomberg.com/assets/dl/dlws.wsdl)

## Quick start
1. Create and edit `examples/config.bulk.yaml`
2. Put ids in `examples/ids.sample.csv` (or use inline)
3. Put fields in `examples/fields.sample.txt` (or use inline)
4. Run:
```bash
pip install -e .
bbg-dlws run -c examples/config.bulk.yaml --dry-run
```

> NOTE: `submitter.py` and `normalize.py` are scaffolded. Wire to actual DLWS operations next.
